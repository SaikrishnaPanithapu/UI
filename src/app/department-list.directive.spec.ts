import { DepartmentListDirective } from './department-list.directive';

describe('DepartmentListDirective', () => {
  it('should create an instance', () => {
    const directive = new DepartmentListDirective();
    expect(directive).toBeTruthy();
  });
});
