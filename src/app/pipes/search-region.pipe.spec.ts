import { SearchRegionPipe } from './search-region.pipe';

describe('SearchRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
